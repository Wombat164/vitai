- The shipped demo exercises contract 51. Two weigh-ins in the travel week now
  carry `absent_fields: kg` with `absent_reason: not-performed`, written through
  `generate_demo.py` like everything else in that record. `examples/demo` had
  zero rows with a stated absence, so a conformance client could rebuild it at
  contract 53, render all six absence reasons as one grey square, and pass every
  job it has - a fixture that does not exercise a feature cannot prove a client
  handles it. The reason code comes from the record rather than the author: the
  context row for that week says "no scale, no gym, hotel treadmill only", so
  nothing was attempted and nothing failed to come back.
- A field-level register on the demo, beside the dataset-level one from #359:
  every field some fixture writes either reaches `examples/demo` or is listed
  with a reason that says whether it is a DECISION or a GAP. It is scoped to the
  class the pooled `test_field_population` measurement hides - a field one
  persona writes reads as written there, and the demo is the fixture downstream
  clients actually build from - and the two registers are held disjoint by a
  test, so one fact never sits in two lists. Verified by execution: with the two
  rows above removed it names `weight.absent_fields` and `weight.absent_reason`,
  which is the defect it was written for.
- Contract 52 is unexercised in the demo too, and stays that way with the reason
  recorded. Its one comparability row is `comparable` over two same-day pairs
  taken at 07:15 and 19:33 against the clinic's 10:15, and this record's own
  rollup refuses to read its weight trend because weigh-ins span 12.3 h, which
  alone accounts for about 1.02 kg - the size of the whole apparent disagreement.
  Publishing that as `difference_lo`/`difference_hi` would state a
  cross-instrument range the record cannot separate from the athlete's own day.
