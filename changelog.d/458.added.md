- `vitai energy-agreement` and `Vitai.energy_agreement()`: whether a record's
  own energy balance explains its own weight change better than its own median
  does. It never states a centre and never applies a model - the fitted model
  is scored out of sample and thrown away. The answer on every record this
  repository holds is no, or that the record cannot be asked.
