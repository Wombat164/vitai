- `vitai rate-uncertainty` and `Vitai.rate_uncertainty()`: the phase-0
  measurement behind `weight_rate` being scored as a direction rather than a
  number, run on any record. The counting script that produced the original
  1.74 lived in a private tooling directory by design, so the figure had never
  been reproducible here.
