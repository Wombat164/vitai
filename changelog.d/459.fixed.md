- The restatement detector now reads the `min_spread_abs` its own registry
  declares. It has always implemented exact equality - a run of identical
  values - which is strictly stronger than the rule `semantics/variation.toml`
  states, so a weight series creeping by a gram a day walked past a check
  whose own comment says it should not. The declared floor had no code
  consumer at all.
