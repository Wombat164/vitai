- `validate`'s advice for a position two machines stamped is now actionable.
  It said "no correction can name one of them - restate the value on a new line
  rather than correcting these", which was true when nothing could name them and
  is false now that `supersedes_device` can. The old sentence survives for the
  case the new field cannot reach: occupants written by ONE machine, or by none,
  which is what a record predating devices looks like.
