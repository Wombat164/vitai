- `vitai bands` and `schema()["bands"]`: what every interval-shaped field in
  this schema claims, and where its width came from. Two closed vocabularies -
  a BASIS saying where the width came from, which is #402's earned-ness rule,
  and a COVERS word whose every member states the coverage out loud, because
  `lo`/`hi` name none. The two pairs that are NOT bands say so rather than
  being left out of a list a consumer would read as complete.
