- **The rollup's weight table is pinned as a series, one line per day**
  (#396). It was reported as listing claims - a date repeated three times,
  each repeat the same value from a different source. It does not reproduce:
  `Vitai.rollup` and `Vitai.build` both hand the report the canonical weight,
  and the resolver groups `weight` strictly by date with no branch that can
  emit two rows for one day. Six shapes were tried and all collapse.
- What was missing was anything HOLDING it there. The table reads canonical by
  a call-site argument, and the report renders whatever list it is given, so
  three words in `api.py` could have turned the table into a claim log with
  nothing failing. The tests now cover the agreeing-value case first, because
  that is the one where two lines are arithmetically unimpeachable and still
  wrong about how many times the athlete stood on a scale.
- The coverage footer's weight count and the rolling average are pinned
  alongside it. The average means over the values in its window, so a day
  carrying three claims would count three times - invisible when they agree,
  and a drag toward whichever day was logged most when they do not.
