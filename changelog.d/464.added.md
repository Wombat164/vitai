- A touch entry may carry `unless = "<condition>"`, naming a public read on
  `Vitai` and a predicate over its answer. A conditional row still forces a
  move until the condition is evaluated against a real record: `vitai
  contract-impact --root <record>` and `Vitai.contract_impact()` can excuse
  one, the rootless door cannot, and silence never does.
