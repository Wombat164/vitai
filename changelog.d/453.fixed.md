- A client read-set naming surfaces this engine does not publish is now
  refused rather than silently matching nothing. A read-set of typos earned
  "you may stay", which is the fail-open an empty read-set was already closed
  against, one argument to the left.
- `report:` surfaces backed by a `property` rather than a method resolve. A
  `property` object is not `callable()`, so `report:policy` was reported as
  naming nothing while a real client read it and this engine published it.
