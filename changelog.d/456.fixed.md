- The payload already carried an absence vocabulary and it was the wrong one:
  `builds.absence_meanings` is about a field missing FROM A BUILD, none of its
  words is a legal `absent_reason`, and `unknown` is precisely the word
  contract 51 dropped. Each list now says which one it is.
