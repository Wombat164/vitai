- **The engine published `container: false` for `inferences.depends_on`, which
  is a list** (#386). `api.field_types` reports that flag straight off
  `db.LIST_COLS`, and the accessor exists (#257) so a consumer does not have
  to guess which TEXT columns need decoding. A consumer trusting it read the
  JSON array as a scalar string, which drops the justification link rather
  than failing. The stored text was never mangled - what was wrong was the
  contract published about it.
- It had been a declared list since inferences generation 2, and no corpus row
  had ever carried one, so nothing had cause to notice. It was found by
  writing the first row that does.
- **The demo gains a third inference**, resting on exactly two claims and
  naming both. The other two could not carry the link honestly: one rests on
  two weeks of rows and the other on the record's full range, so any list
  short enough to read would be a partial justification presented as a whole
  one. Its numbers are read off the two rows it cites rather than restated,
  and both ids resolve to real rows. `inferences.note` and `recorded_at` are
  populated alongside it, and the two that gained writers have left #204's
  `UNWRITTEN` register.
