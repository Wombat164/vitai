- A field absent from `bands` has no declared width, and that is an answer
  rather than a gap: nothing supplies a default interval for a field whose
  uncertainty nobody measured. No new band is invented by this change.
