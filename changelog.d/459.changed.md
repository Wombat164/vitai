- `docs/persona-doctrine.md` gains a sixth property: a persona's data must be
  able to exhibit the phenomena the engine detects. A fixture that cannot
  passes every test written against it and proves nothing, and a corpus
  regenerated in the same change as the gate that judges it cannot falsify
  that gate.
