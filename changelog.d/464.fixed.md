- Contract 54's `meta:supersedes_seq` narrowing is now conditional. It is real
  for a record two machines have written and for no other, because a position
  can only be contested where two machines wrote it - so the surface table and
  the migration note in `README.md` stop disagreeing about the same contract.
