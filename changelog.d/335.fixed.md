- An MCP tool declared with `method: None` now runs the module-level API
  function of its own name, which is what the adapter has always documented.
  It returned `schema()` whatever the tool was called - harmless while
  `schema` was the only rootless tool, and a silent wrong answer for the
  second one.
