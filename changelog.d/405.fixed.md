- **The persona validator asked the schema about a dataset called
  `daily.old-band`** (#405). `test_every_data_line_validates` derived the
  dataset from the whole filename stem, so any of #105's per-device streams
  raised `KeyError`. Nothing had noticed because no persona used a second
  stream until now - and a one-time import is a second writer by nature, so
  the shape #405 is about could not be fixtured without hitting it. The
  dataset is the stem before the first dot; `vitai validate` had it right all
  along.
