- `payload:key[.name]` as a fourth contract-impact surface grammar, naming what
  `api.schema()` publishes, so a client can declare that it reads the engine's
  vocabulary. No contract row may use it - the payload does not move with the
  contract number - and a test refuses one that tries.
- `payload_digest` in `vitai schema --json` and on every `contract-impact`
  verdict: one bit that changes when the published shape does and stands still
  for the engine version, answering the question the contract number cannot.
