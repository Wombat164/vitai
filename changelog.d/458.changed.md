- The forward centre for weight is decided AGAINST rather than deferred: the
  input error on `kcal_out` is unquantified and may exceed the signal, the
  selection bias in logged days is unreachable from inside a record, and no
  record here can tell a model that works from one that does not. See
  `docs/proposals/weight-outlook.md` for the measurement and for the three
  conditions that would change it.
