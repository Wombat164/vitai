- The demo corrects a mis-entered threshold, so `thresholds.change_kind` shows
  both of its two values. Every threshold edit in the demo and all fifteen
  personas was a `change`, which meant `policy._edits`' skip for a correction
  had never executed for this dataset and no client had been made to tell the
  two apart. `goals.change_kind` already showed both, which is what said the
  shape was expressible and this dataset simply had no instance of it.
- A protein target typed with a trailing zero in the onboarding sitting - 1450
  where the interview said 145 - and corrected in the same sitting. Both lines
  carry the same date, so the mis-entry is superseded on the day it was written:
  no day of the record ever resolves to 1450, and the effective policy history
  is unchanged everywhere.
- What the register entry could not say, because nothing had run it: marked
  `change` instead, that pair reports `protein_g_target` LOOSENED on day one -
  the largest apparent policy retreat in the record, invented entirely by a
  typo. That is the churn G31 exists to keep out of the audit trail, and a test
  asserts both directions on the shipped fixture rather than only the quiet one.
- `thresholds.change_kind` leaves `VOCAB_SINGLE`, which takes that register from
  six entries to five. It is the first to leave by being fixed rather than
  re-argued; the four `AUTHORS` entries still want a record with a coach, which
  is a corpus decision rather than a fixture edit.
- The demo holds a fixture the athlete did not start, so `events.outcome` shows
  both of its two values. Every event in the demo and all fifteen personas took
  place, which meant a client that READS the outcome and one that assumes it
  were indistinguishable over this corpus - and the field's own doctrine warns
  that rendering an unanswered outcome as a miss "accuses an athlete of skipping
  a race that has not happened yet". Nothing here could have caught a client
  that did.
- A club 10k on 2030-06-02, entered in April, that went ahead while she was
  away for work. The record already says so from three other directions: the
  context row calls that week travel, the weigh-in that morning says it did not
  happen, and the day before it has no row at all. One cause, four honest
  records of it.
- The register entry said an event with no attendance "moves the plans that were
  built backwards from it", and the argument it was waiting for turned out to be
  about WHICH event rather than whether. The demo's two existing fixtures are the
  date the whole block is planned backwards from and the hard date a goal points
  at, and either would have moved; a priority-`c` club race that nothing points
  at moves nothing. The rollup's "Coming up" does not list it, because it is in
  the past, and a test holds that no goal or plan names it.
- The why is in `note`, not `reason`. `reason` explains a fixture that was
  CANCELLED or MOVED - `maja`'s gym closure, called off when the works were
  postponed - and this one went ahead without her, so `events.reason` stays
  registered as absent from the demo.
- `events.outcome` leaves `VOCAB_SINGLE`, five entries to four. The four that
  remain are one fact: `AUTHORS` distinguishes athlete from coach and no record
  in this corpus has a coach.
