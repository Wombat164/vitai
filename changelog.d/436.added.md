- The demo corrects a mis-entered threshold, so `thresholds.change_kind` shows
  both of its two values. Every threshold edit in the demo and all fifteen
  personas was a `change`, which meant `policy._edits`' skip for a correction
  had never executed for this dataset and no client had been made to tell the
  two apart. `goals.change_kind` already showed both, which is what said the
  shape was expressible and this dataset simply had no instance of it.
- A protein target typed with a trailing zero in the onboarding sitting - 1450
  where the interview said 145 - and corrected in the same sitting. Both lines
  carry the same date, so the mis-entry is superseded on the day it was written:
  no day of the record ever resolves to 1450, and the effective policy history
  is unchanged everywhere.
- What the register entry could not say, because nothing had run it: marked
  `change` instead, that pair reports `protein_g_target` LOOSENED on day one -
  the largest apparent policy retreat in the record, invented entirely by a
  typo. That is the churn G31 exists to keep out of the audit trail, and a test
  asserts both directions on the shipped fixture rather than only the quiet one.
- `thresholds.change_kind` leaves `VOCAB_SINGLE`, which takes that register from
  six entries to five. It is the first to leave by being fixed rather than
  re-argued; the four `AUTHORS` entries still want a record with a coach, which
  is a corpus decision rather than a fixture edit.
