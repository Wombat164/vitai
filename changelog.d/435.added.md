- `hana`'s band writes the zero it did not mean, and `false_zero_questions` has
  a true positive for the first time. The rule was built, argued and tightened
  twice against a corpus that could not exercise it: its own docstring recorded
  that it "produced no true positive anywhere, because no corpus record contains
  the shape this kind exists for", and measured over the demo and all fifteen
  personas the kind fired zero times while `outage` fired once. A detector whose
  detection nobody has watched work is a specification.
- The row is one line of history, and the narrative was already there. `WORLD.md`
  has always said the band was "worn from the previous June until the end of
  December, when the strap perished"; its last day now records zero steps. She
  walks the long way to everything, which is the first line of her profile, so
  the zero is the instrument failing rather than the day being empty.
- Why that band and not a watch: the rule fires on a source contradicting
  ITSELF, so it needs a long unbroken habit of non-zero counts - this one has
  105 before the zero - and it counted steps and nothing else, which is the pure
  case for a register holding exactly `steps`. The dying-watch shape the engine
  describes also carries a near-floor `kcal_out` beside the zero, which this
  band never wrote; that half stays unexercised and the expectation row says so.
- The archive is now a refusal and a detection at once. No `outage` question
  names it, because a one-time import never had a transaction cadence to break -
  the false positive #405 was filed for - and a `false_zero` question does,
  because it reported a count it did not have. Two failure modes of one channel,
  in one record, asked about differently.
- The corpus anchor asserted ONE instrument question, which guarded `outage`'s
  detection and left `false_zero`'s unrun. It asserts both now, one of each kind,
  so a change that silences either has broken a detection and a change that adds
  a third has broken a refusal.
