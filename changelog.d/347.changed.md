- **Changelog entries are fragments, not edits to one contended block.** Every
  PR appended under the same heading, so any two open at once conflicted on the
  same lines - four times in two days, every one additive and resolved by
  keeping both. A PR now writes `changelog.d/<issue>.<category>.md` and never
  touches `CHANGELOG.md`; a maintainer folds them in at release. Checked by
  `scripts/changelog_gate.py`.
