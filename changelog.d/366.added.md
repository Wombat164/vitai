- The README's data-model tables named 16 of `schema.KEYS`'s 22 datasets;
  `plans`, `capabilities`, `instruments`, `protocols` and `regimes` are now
  listed (in whichever table matches what they record), and `emissions` is
  registered as deliberately absent - it has one door, `assert_delivery`,
  and a writer-facing table pointing at it would be routing readers
  somewhere the engine itself refuses to let them write.
- `skills/vitai-ingest/SKILL.md` no longer names three datasets as if they
  were the whole schema; it now points at `vitai schema`, which lists every
  current dataset and cannot go stale the way a hand-written list did.
- Added `tests/test_readme_dataset_coverage.py`: an allowlist gate, matching
  the shape of `test_fixture_coverage.py`, that fails closed if a dataset in
  `schema.KEYS` is neither named in the README's data-model tables nor
  registered with a reason for its absence (#366).
