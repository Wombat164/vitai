# What cuts a release, pinned (#416).
#
# The release path was the least pinned thing in the repo and it only runs on
# a tag, which is the worst combination: breakage surfaces at release time, on
# a `main` that every other job reports green, at the moment there is least
# appetite to debug a toolchain.
build==1.5.0
twine==7.0.0
